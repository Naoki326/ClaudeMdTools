# lanbook

把散落硬盘的 Markdown 笔记与 HTML 课程原地托管成局域网 Web 服务：文件不动、实时刷新、手机 / 平板跨设备阅读。

## Language

**数据目录**:
服务自身状态（配置、metadata）的家，位于用户主目录下 `~/.lanbook/`。安装目录只读——服务从不向自己代码所在处写数据。
_Avoid_: 配置目录（它不止放配置）、安装目录

**安装模式 / 源码模式**:
同一份代码的两种运行身份——npm 全局安装后运行（安装模式），或 git 仓库内直接运行（源码模式）。

**根目录**:
被服务扫描、原地读取的外部文件夹，文档内容的来源，由用户在配置中添加。与数据目录相对：根目录是被看的内容，数据目录是服务自己的家。
_Avoid_: 数据目录、项目目录

**迁移**:
首次以 1.2.0 启动时，把旧位置（安装目录）的配置与 metadata 一次性拷入数据目录，旧文件改名留底。单向，只发生一次。

**服务配置**:
数据目录下描述服务自身行为的配置（端口、监听地址），与内容类配置（根目录）相对。改动重启后生效。
_Avoid_: 全局配置、应用配置
